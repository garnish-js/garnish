import {CommandContainer} from './command';
import {IGanish} from '@garnish/common';

export class Garnish implements IGanish{
  constructor(container: CommandContainer) {
    //
  }

  public run() {
    //
  }
}
